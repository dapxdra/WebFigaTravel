// Central place for SEO constants shared between index.html (static) and the
// usePageMeta hook (dynamic, per-route). Keep in sync with index.html if the
// business identity changes.
export const SITE_URL = 'https://figatravelcr.com'
export const SITE_NAME = 'Figa Travel Costa Rica'
export const DEFAULT_OG_IMAGE = `${SITE_URL}/assets/home/hero-header.png`

export const DEFAULT_KEYWORDS =
  'Costa Rica private transportation, Costa Rica airport shuttle, private transfers Costa Rica, Costa Rica tourist transportation, van rental with driver Costa Rica, La Fortuna airport transfer'
